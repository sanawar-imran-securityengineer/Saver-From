version: '3.8'

services:
  saverfrom:
    build: .
    container_name: saverfrom-app
    restart: always
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
      - NODE_ENV=production
